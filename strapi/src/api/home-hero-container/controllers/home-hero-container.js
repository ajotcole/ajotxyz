'use strict';

/**
 * home-hero-container controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::home-hero-container.home-hero-container');
